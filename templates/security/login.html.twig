{% extends 'base.html.twig' %}

{% block body %}
	{% if error %}
		<div class="alert alert-danger">
			{{error.messageKey}}
		</div>
	{% endif %}

	{# {{form_start(form)}}
		{{form_rest(form)}}
		<input type="submit" value="Connexion" class="btn btn-success mt-2"> #}

	<form class="container" method="post">
		<label for="username" class="form-label">Nom d'utilisateur</label>
		<input type="text" name="_username" id="username" value="{{ last_username }}" class="form-control" required>
		<label for="password" class="form-label">Mot de passe</label>
		<div class="input-group">
			<input type="password" name="_password" id="user_password" class="form-control" required>
			<span role="button" class="input-group-text" onclick="togglePassword()">
				<i class="bi bi-eye" id="eye"></i>
				<i class="bi bi-eye-slash d-none" id="eye-slash"></i>
			</span>
		</div>
		<input type="hidden" name="_target_path" value="/">
		<input type="hidden" name="">
		<input type="submit" value="Connexion" class="btn btn-success mt-2">
	</form>
	<a href="/user/new" class="btn btn-link">Vous n'avez pas encore de compte ?</a>

{% endblock %}


{# /* <style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Hello ✅</h1>

		This friendly message is coming from:
		<ul>
			<li>Your controller at
				<code>
					<a href="{{ 'C:/MAMP/htdocs/reseau-studi/src/Controller/SecurityController.php'|file_link(0) }}">src/Controller/SecurityController.php</a>
				</code>
			</li>
			<li>Your template at
				<code>
					<a href="{{ 'C:/MAMP/htdocs/reseau-studi/templates/security/index.html.twig'|file_link(0) }}">templates/security/index.html.twig</a>
				</code>
			</li>
		</ul>
	</div> */ #}
