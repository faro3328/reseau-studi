{% extends "base.html.twig" %}

{% block body %}
	{{ form_start(form) }}
	{{ form_rest(form) }}
	<input class="btn btn-primary" type="submit" value="Publier">
{% endblock %}
{# {{ form_row(post_form.title) }}
			{{ form_row(post_form.content) }}
			{{ form_row(post_form.image) }} #}
{# //equivaut au 3 lignes du dessus #}
{# {{ form_end(post_form) }} #}
